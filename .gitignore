# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Environment
.venv/
venv/
.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
nul

# Quality reports (generated output — not source code)
.quality-reports/
qg.sarif
ck-report.md
ck-report.json

# SQLite graph cache
.cathedral-keeper/graph.db

# Harness runtime cache (reuse-injector index — derived, per-machine)
.harness/cache/

# Observatory runtime data (captured events + maturity history — per-machine, never committed)
.observatory/

# Claude Code — project settings + ctx ledger are shared (committed); rest stays local
.claude/*
!.claude/settings.json
!.claude/ctx/

# Temp
*.tmp
*.bak

# Internal working docs — local-only, never published
docs/internal/
docs/deliverables/
docs/plans/
